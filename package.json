{
  "name": "object-diff",
  "version": "0.0.1",
  "description": "Generate difference between two objects. Only looks at structural and type differences, not if same keys have changed. i.e. assumes immutable structures",
  "main": "index.js",
  "scripts": {
    "test": "ava --watch"
  },
  "keywords": [],
  "author": "mcMickJuice",
  "license": "ISC",
  "devDependencies": {
    "ava": "^0.14.0",
    "babel-core": "^6.7.6",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-stage-0": "^6.5.0",
    "babel-register": "^6.7.2"
  },
  "ava": {
    "files": [
      "spec/**/*spec.js"
    ],
    "source": [
      "src/**/*.js"
    ],
    "require": [
      "babel-register"
    ]
  },
  "babel": {
    "presets": [
      "es2015",
      "stage-0"
    ]
  },
  "dependencies": {
    "lodash": "^4.11.1"
  }
}
